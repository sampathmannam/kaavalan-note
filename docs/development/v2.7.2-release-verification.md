# KaavalanNote v2.7.2 release verification — 2026-09-14

Release tag `v2.7.2` resolves to `a001cb938c687a8b9e3e1cefcaa97ef408a7eb28`,
following the end-to-end hardening commit `15c17448bf9a`. The package remains
`com.kaavalan.note`, versionName is `2.7.2` and versionCode is `55`. Room remains at
schema 18 and the manual backup format remains at schema 4.

## Pre-release gates

- The hardening pass completed 882 JVM tests: 870 passed, 12 intentional skips and
  zero failures or errors.
- Android lint completed with zero errors. Debug and optimized R8/resource-shrunk
  release variants assembled successfully.
- All 26 Android 14/API 34 connected-device scenarios passed in the isolated
  `com.kaavalan.note.debug.officer` package.
- The v2.7.2 bundled changelog test passed after the version/code bump.
- Deno tests and formatting passed. Gitleaks, verified TruffleHog, Trivy and SonarQube
  reported no active secret, vulnerability, bug or security hotspot. Reviewed Semgrep
  and OpenGrep findings were limited to required Android entry points and AES-GCM
  nonce-handling paths.

The detailed security-tool scope and limitations are recorded in
`end-to-end-hardening-2026-09-14.md`.

## Signed artifact integrity

The existing private production configuration was read only from the separate
production checkout. No credential or keystore was printed, copied, regenerated,
changed or committed.

| Check | Result |
|---|---|
| Published universal APK | 72,048,214 bytes |
| Published SHA-256 | `93127f7cee27c4b798b1bf01280e962643ceeaf3f29d4e4731d2415903dcc098` |
| Package metadata | `com.kaavalan.note`, code 55, version 2.7.2, target API 35 |
| APK signature | Verified with APK Signature Scheme v2; one signer |
| Certificate SHA-256 | `e1a04e4840490fd920fe719f6d2e4d5ac8f09c248a19c6a4359bf914baa31a70` — matches the repository pin |
| Alignment | `zipalign -c -P 16 4` passed |

## Signed in-place upgrade

The task-owned `kaavalan-test` Android 14/API 34 arm64 AVD was started in read-only
mode. Its signed v2.7.1/code-54 `com.kaavalan.note` installation already contained the
synthetic instruction `Release260UpgradeMarker`. The pre-publication candidate passed
first. Because the guarded release script produced a fresh signed build, the test was
then repeated against the exact final APK whose digest matches the public GitHub asset.

Installing the final signed v2.7.2 APK with `adb install -r --no-streaming` returned
`Success`. The package advanced to code 55 while its original installation timestamp
remained `2026-09-06 23:04:48`. After a force-stop, the updated app completed a cold
launch and Today still displayed the exact marker instruction. The collected logcat
contained no application fatal exception. The read-only emulator session was stopped
after verification, so its base image was not changed.

The six guarded release-script gates passed and GitHub published one Obtainium asset:
`kaavalan-note-2.7.2.apk`. The GitHub asset digest and the locally verified final APK
digest are identical.

GitHub tag CI run `34801173225` completed successfully. Its unit-test, Android lint,
debug assembly, R8 release assembly, release-artifact and arm64 size-budget gates all
passed. The connected-device job emitted its designed skip notice because that runner
had no configured test emulator; it did not claim a cloud device run. The complete
26-scenario Android device suite and the two signed-upgrade checks above were performed
locally on the task-owned emulator.

This verifies the app-side signed-upgrade contract with synthetic data on one Android
14 emulator. It does not claim a physical-phone matrix, live Google OAuth/Drive
exchange, TalkBack certification or speech-recognition accuracy across OEM devices.
