# KaavalanNote v2.7.6 release verification — 2026-09-21

KaavalanNote v2.7.6/code 59 adds instruction provenance for work assigned to the
officer. The package remains `com.kaavalan.note`; Room advances from schema 18 to
19 and the manual-backup format advances from schema 4 to 5.

## Guarded publication

Release tag `v2.7.6` resolves to
`60a6864ae16777f5179697e97d9338cb9232a36e`. The six guarded release gates all
passed: clean source and an unused tag, configured production signing, the complete
debug JVM suite, versionCode 59 greater than published code 58, APK signature
verification, and an exact match to the pinned production certificate.

| Check | Result |
|---|---|
| Release | [KaavalanNote 2.7.6](https://github.com/sampathmannam/kaavalan-note/releases/tag/v2.7.6) |
| Universal asset | `kaavalan-note-2.7.6.apk`, 72,094,036 bytes |
| SHA-256 | `e1d27d27e84be870f706ce4ba4dadfda481bbe2485ed40b4600684d0864de7f8` |
| GitHub asset digest | Exact match to the locally verified signed artifact |
| Package | `com.kaavalan.note`, code 59, version 2.7.6, min API 26, target API 35 |
| Native ABIs | `arm64-v8a`, `armeabi-v7a`, `x86`, `x86_64` |
| APK signature | APK Signature Scheme v2; one signer |
| Certificate SHA-256 | `e1a04e4840490fd920fe719f6d2e4d5ac8f09c248a19c6a4359bf914baa31a70` — exact repository-pin match |

The private signing values were used only by the opted-in release process. They were
not printed, copied into the development checkout, regenerated or committed.

## Verification

| Gate | Result |
|---|---|
| JVM unit tests | 906 passed; 0 skipped, failed or errored |
| Android lint | Debug lint passed with 0 errors/fatals |
| Release build | R8, resource shrinking and release-vital lint passed |
| Database | Room 18 → 19 migration is registered without destructive fallback |
| Backup | Schema 4 → 5 round-trip and earlier-format compatibility are covered by the JVM suite |
| Tag CI | [Run 35607459073](https://github.com/sampathmannam/kaavalan-note/actions/runs/35607459073): lint, JVM tests, debug assembly and R8 release assembly passed; the device job is red because one test retained superseded button copy |

Before release preparation, the v18 → v19 upgrade and the new **Who assigned this
to you?** control were exercised in the isolated debug package on a connected
Motorola Signature running Android 17/API 37. Selecting SP populated the issuer
field, and no matching Room, SQLite or application fatal was recorded.

## Signed in-place upgrade

The exact signed APK later uploaded to GitHub was installed with `adb install -r
--no-streaming` on the dedicated `kaavalan-test` Android 14/API 34 AVD in read-only
mode. It upgraded the existing signed production package from v2.7.1/code 54 to
v2.7.6/code 59, exercising the same Room 18 → 19 path used by v2.7.5.

The original installation timestamp remained `2026-09-06 23:04:48`, the retained
synthetic `Release260UpgradeMarker` instruction was visible before and after the
upgrade, and the updated app completed a cold launch without a matching fatal,
SQLite or Room migration error. The AVD was started read-only so its base image was
not changed.

## Physical signed upgrade

The connected Motorola Signature (`vantage`, Android 17/API 37) started with the
signed production v2.7.5/code 58 installation. The exact published APK updated it
to v2.7.6/code 59 with `adb install --no-streaming -r`; Android returned `Success`.
The original installation timestamp remained `2026-09-08 12:35:20`, confirming an
in-place update rather than a reinstall. A cold launch completed in 330 ms, the app
process remained alive, and the last 2,000 log lines contained no matching fatal,
SQLite or Room migration error. No uninstall, app-data clear or private database,
note or contact inspection was performed.

## Tag-CI device-test disposition

The first connected-device attempt compiled successfully but ADB timed out while
probing an unrelated emulator and returned `0 tests` / `No test results`. A failed-job
rerun then executed all 43 discovered cases: 40 passed, two environment-specific
tests were skipped as designed, and one test failed before exercising its workflow
because it still searched for the old `Link a contact (optional)` button copy.

The product intentionally changed that copy to `Link a related contact (optional)`
to distinguish the related/responsible contact from the new assigning officer. The
test selector was corrected after the immutable release tag without changing the app
source. The affected Compose → ViewModel → encrypted Room → WorkManager scenario was
then rerun alone on the dedicated read-only Android 14 AVD and passed 1/1 with zero
skips or failures. Because the tag's stale selector keeps the aggregate step red, the
workflow correctly skipped its external forced-Doze probe; this record does not
represent the tag run itself as green.

## Scope

This patch verifies instruction-issuer capture, persistence, editing, search,
display, migration and backup behavior. It does not claim a multi-OEM device matrix,
live Google Drive account round trip, TalkBack certification or a multi-day soak.
