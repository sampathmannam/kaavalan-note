# KaavalanNote v2.7.4 release verification — 2026-09-15

KaavalanNote v2.7.4/code 57 is the reliability follow-up to v2.7.3/code 56.
The package remains `com.kaavalan.note`; Room schema 18, manual-backup schema 4,
target API 35 and the pinned production signing identity are unchanged.

## Guarded publication

Release tag `v2.7.4` resolves to
`797ba7a6d990ab1cc18e9fd9b97a3f2dabe95010`. The production-signing worktree was
clean and detached at that commit. `release.sh 2.7.4` passed each fatal gate:

1. clean tree and a new local/GitHub tag;
2. configured production signing key;
3. all 886 debug JVM tests;
4. code 57 greater than the published code 56;
5. signed universal APK verification; and
6. the existing pinned certificate fingerprint.

The script then pushed the annotated tag and published a stable, non-draft GitHub
release. No signing credential was printed, committed, or copied into the development
checkout.

| Check | Result |
|---|---|
| Release | [KaavalanNote 2.7.4](https://github.com/sampathmannam/kaavalan-note/releases/tag/v2.7.4) |
| Universal asset | `kaavalan-note-2.7.4.apk`, 72,068,283 bytes |
| SHA-256 | `bb0c0dd6b774054e3da1e5095f37a1ed6d34e658939c63af25f6e4efdf97ec5d` |
| GitHub asset digest | Exact match to the local SHA-256 |
| Package | `com.kaavalan.note`, code 57, version 2.7.4, min API 26, target API 35 |
| Certificate | Pinned production SHA-256 `e1a04e4840490fd920fe719f6d2e4d5ac8f09c248a19c6a4359bf914baa31a70` |

## Evidence carried into the tag

The exact tagged source had already passed 886 JVM tests across 156 suites with no
skips, failures or errors. Android lint had zero errors or fatals; debug and optimized
release variants assembled. The 41-test Android aggregate passed 39 tests with two
environment-specific skips and no failures on Android 14/API 34 and a Motorola Android
17/API 37 device. The host-driven forced-Doze reminder probe passed after process death
on both devices.

Gitleaks found no secrets in the current tracked/unignored snapshot. Trivy reported no
vulnerabilities, secrets or misconfigurations in that source snapshot. Detailed test
and limitation evidence remains in `end-to-end-hardening-2026-09-14.md`.

GitHub tag CI started after publication. Its result is separate from the completed local
release gates; no claim is made here about a physical-device matrix, TalkBack
certification, a multi-day soak, or live Google Drive account exchange.
