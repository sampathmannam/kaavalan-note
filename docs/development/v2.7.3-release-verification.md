# KaavalanNote v2.7.3 release verification — 2026-09-14

The user explicitly requested a release of the approved plum theme. The package
remains `com.kaavalan.note`, versionName advances to `2.7.3` and versionCode to `56`
from the latest published v2.7.2/code 55. Room stays at schema 18 and the manual
backup format at schema 4. No dependency or signing-identity change is intended.

## Pre-release gates

- The versioned source passed 885 JVM tests across 158 suites: 873 passed,
  12 existing skips, zero failures and zero errors. This includes the bundled
  changelog and expanded light/dark contrast checks.
- Android lint passed with zero errors/fatals, 341 warnings and 556 informational
  findings. Release-vital lint also passed.
- The palette implementation previously passed six Android 14/API 34 device
  scenarios with Android Test Orchestrator, covering status colours, light/dark
  workspace, large text, landscape, keyboard-safe capture and follow-up workflow.
  See `plum-theme-verification-2026-09-14.md`. The broader 27-scenario device suite
  was not rerun for this UI-only release.
- The staged palette/version/release-note diff passed Gitleaks with zero findings,
  and `git diff --cached --check` and `bash -n release.sh` passed.

## Local build-output repair

The first signed release build stopped at R8: a stale incremental transform had
omitted `KaavalanApplication_GeneratedInjector.class`, although it existed in
the release javac output. Only
`app/build/intermediates/classes/release/transformReleaseClassesWithAsm` was moved
to a temporary backup. Regenerating it with the build cache disabled restored the
class and R8 passed. No source, keep rule or test assertion was changed to bypass
the failure. Builds used JDK 17, isolated Gradle/Android/test homes, one worker,
in-process Kotlin compilation and a 1536 MiB heap. Unrelated processes were not
stopped.

## Signed artifact and publication

Release tag `v2.7.3` resolves to
`67a80c263ca82ff96de121ee53ceae2f874c7cd9`. All six guarded `release.sh` gates
passed, and GitHub published the stable, non-draft release on 2026-09-14.
The script's final APK is byte-for-byte identical to the post-commit candidate
used for the signed upgrade below; GitHub's asset digest also matches.

| Check | Result |
|---|---|
| Universal asset | `kaavalan-note-2.7.3.apk`, 72,064,957 bytes |
| SHA-256 | `7891a2e63bf1d88825186c5793c32bc4ee965913d58298ee632b01de3023c4db` |
| Package | `com.kaavalan.note`, code 56, version 2.7.3, target API 35 |
| Signature | APK Signature Scheme v2 verified, one signer |
| Certificate SHA-256 | `e1a04e4840490fd920fe719f6d2e4d5ac8f09c248a19c6a4359bf914baa31a70`, unchanged repository pin |
| Alignment | `zipalign -c -P 16 4` passed |
| ABIs | arm64-v8a, armeabi-v7a, x86, x86_64 |
| arm64 APK size | 25,315,926 bytes, below the existing 40 MiB CI budget |

The private signing configuration was read only from the existing production
checkout. Credentials were passed in process memory, not printed or copied into
the development configuration. The original key and pin were not changed.

## Signed in-place upgrade

The task-owned Android 14/API 34 arm64 `kaavalan-test` AVD ran in read-only mode
on `emulator-5596`. Its existing signed installation was first updated to the
downloaded, checksum-verified v2.7.2/code-55 release. Today displayed the existing
synthetic `Release260UpgradeMarker` instruction before testing the new version.

`adb install -r --no-streaming` of the exact final v2.7.3 APK returned `Success`.
After a force-stop and cold launch, the app displayed the same instruction with
the new plum theme. The package advanced from code 55 to 56 and kept the original
`firstInstallTime` of `2026-09-06 23:04:48`. The collected crash buffer was empty.
Native before/after screenshots were inspected; XML, package metadata and images
are retained locally in `app/build/ui-qa/v2.7.3/`.

No production package was uninstalled or cleared. The task-owned read-only
emulator was stopped after verification; its base image and unrelated devices
were not changed.

## CI handoff

[GitHub tag CI run 34837034869](https://github.com/sampathmannam/kaavalan-note/actions/runs/34837034869)
completed successfully after publication. This record does not claim a cloud device
run. Local unit, lint, signed-release, device-subset and signed-upgrade evidence is
recorded above.

This record does not claim a physical-device matrix, TalkBack certification,
live Google OAuth/Drive exchange or speech accuracy across OEM devices.
