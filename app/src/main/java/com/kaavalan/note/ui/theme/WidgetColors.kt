package com.kaavalan.note.ui.theme

import androidx.glance.material3.ColorProviders

/** Widgets keep the plum identity instead of inheriting an unrelated wallpaper hue. */
internal val KaavalanWidgetColors = ColorProviders(
    light = KaavalanNoteLightScheme,
    dark = KaavalanNoteDarkScheme,
)
